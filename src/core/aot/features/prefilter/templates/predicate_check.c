#include <stddef.h>
#include <string.h>

static inline int pf_predicate_check_template(void) {
  int seen_count = 0;
  const char* value_start = NULL;
  size_t value_len = 0;
  int bool_val = 0;
  double number = 0;
  (void)seen_count;
  (void)value_start;
  (void)value_len;
  (void)bool_val;
  (void)number;

  /* PF_SNIP_START */
  /* PF_PREDICATE_BODY */
  /* PF_SNIP_END */

  return 0;
}
