#include <stddef.h>
#include <string.h>

static inline int pf_memmem_guard_template(void) {
  const char* line_start = NULL;
  size_t line_len = 0;
  int result = 0;
  (void)line_start;
  (void)line_len;
  (void)result;

  /* PF_SNIP_START */
  /* PF_MEMMEM_BODY */
  /* PF_SNIP_END */

  return 0;
}
